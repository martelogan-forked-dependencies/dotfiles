# Hardcode simple default user agent
user-agent = "curl/7.54.1"

# When following a redirect, automatically set the previous URL as referer.
referer = ";auto"

# Wait 60 seconds before timing out.
connect-timeout = 60
